# Copy this file to .env and add your OpenAI API key
# Without it, Q&A uses TF-IDF fallback (less accurate)
# With it, Q&A uses GPT to generate concise answers from syllabus context
OPENAI_API_KEY=sk-your-key-here
